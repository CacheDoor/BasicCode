#include <stdio.h>
#include <stdlib.h>

//这个书上没有说明白，继续看书再来
struct  list
{
	int data;
	 struct list *next;
};

typedef struct list node;

typedef node *link;

int main(int argc, char const *argv[])
{
	
	return 0;
}